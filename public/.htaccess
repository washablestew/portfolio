Options -MultiViews
RewriteEngine On

# Serve existing files and directories unchanged.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let React Router handle project URLs opened directly.
RewriteRule ^ index.html [L]
